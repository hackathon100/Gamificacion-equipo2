import { schema } from 'normalizr';

/* Schemas: Here you can stablish the relations between each entity on the redux store. This is used to normalize api responses */

// Measurers
// export const measurerSchema = new schema.Entity('measurers');
// export const measurerListSchema = new schema.Array(measurerSchema);
